namespace ENet;

static class ListExtension
{
    //static public void AddLastRange<T>(this List<T> list, T startNode, T? endNode)
    //{
    //    T currNode = startNode;

    //    while (currNode != null && !currNode.Equals(endNode))
    //    {
    //        list.Add(currNode);
    //        currNode = currNode.Next;
    //    }

    //    if (endNode != null)
    //    {
    //        list.AddLast(endNode);
    //    }
    //}

}
